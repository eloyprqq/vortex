# Vortex — 태초마을

이 문서는 게임의 출발점이다.  
구현이 꼬이거나 방향이 흔들리면 여기로 돌아온다. 캐릭터 콘셉트는 네가 가지고 있고, 여기에는 **빈 자리**만 둔다.

**목표:** 브라우저에서 돌아가는 오버워치 스타일 3인칭 히어로 슈터.  
**배포:** GitHub 저장소 `eloyprqq/vortex` → 나중에 GitHub Pages.

---

## 한 줄 정의

짧은 라운드, 역할이 다른 히어로, 3인칭 조준/이동, 스킬 쿨다운.  
지금은 AAA가 아니라 **플레이 가능한 코어**를 먼저 만든다.

오버워치에서 가져올 것:

- 히어로마다 다른 키트 (기본 공격 + 스킬 + 궁극기)
- 역할 (딜 / 탱 / 힐) — 숫자는 나중에 늘린다
- 3인칭 카메라, WASD, 마우스 조준
- 짧은 맵에서 목표(거점 또는 전멸)로 승패

지금 안 가져올 것:

- 계정, 시즌, 스킨 상점
- 서버 매치메이킹, 안티치트
- 시네마틱, 고폴리 캐릭터 스캔
- 6v6 네트워킹 (1단계는 로컬 / 봇)

---

## 기술 뼈대

웹 + GitHub Pages가 전제라서 엔진은 브라우저다.

| 층 | 선택 | 이유 |
| --- | --- | --- |
| 런타임 | 브라우저 (Vite) | Pages에 정적 파일만 올리면 됨 |
| 3D | Three.js | 카메라, 맵, 히트스캔, 이펙트의 기본기 |
| 입력 | Pointer lock + 키보드 | FPS/TPS 조준 |
| 물리 | 직접 구현 (캡슐 이동 + 중력) | 히어로 슈터는 “미끄러운 물리 엔진”보다 예측 가능한 컨트롤러가 중요 |
| 히어로 데이터 | JSON/TS 데이터 파일 | 네가 정한 캐릭터를 코드 수정 없이 꽂을 수 있게 |
| 배포 | `gh-pages` 또는 GitHub Actions → `/docs` 또는 `dist` | 저장소는 이미 `https://github.com/eloyprqq/vortex.git` |

멀티플레이는 **나중 단계**. 1단계는 오프라인: 플레이어 1 + 봇.

---

## 플레이 루프 (항상 이게 심장)

1. 히어로 선택
2. 스폰
3. 이동 / 점프 / 조준
4. 기본 공격으로 맞히기
5. 스킬 사용 (쿨다운)
6. 체력 0이면 리스폰
7. 라운드 목표 달성 또는 시간 종료
8. 다시 선택 화면

이 8칸이 안 돌아가면 맵을 예쁘게 해도 게임이 아니다.

---

## 카메라와 조작

오버워치처럼 **어깨 너머 3인칭**.

- 이동: WASD
- 시점: 마우스 (pointer lock)
- 점프: Space
- 기본 공격: 좌클릭
- 스킬 1 / 2: Shift, E (히어로 데이터에서 덮어쓸 수 있음)
- 궁극기: Q (게이지가 찼을 때)
- 재장전: R (탄약이 있는 히어로만)

카메라 규칙:

- 캐릭터 뒤-옆에서 따라감
- 조준선은 화면 중앙. 총알/빔은 카메라 정면으로 나감 (오버워치식 “카메라 히트스캔”)
- 벽 끼임 시 카메라를 캐릭터 쪽으로 당김

---

## 전투 규칙 (코어)

공통:

- 체력, (선택) 방어막
- 히트박스: 머리 / 몸 (배율은 나중에)
- 히트스캔 또는 발사체 — 히어로 데이터로 선택
- 아군 오사: 1단계에선 끔. PvP 넣기 전에 켠다

역할은 데이터일 뿐, 엔진은 같다.

- **딜:** 높은 DPS, 낮은 체력
- **탱:** 높은 체력 또는 방어 스킬
- **힐:** 아군(또는 자신) 회복 스킬

1단계에서는 힐이 “자신 회복”이어도 된다. 아군이 아직 없으니까.

---

## 캐릭터 슬롯 (네가 채운다)

엔진은 `heroes/` 데이터만 읽는다. 외형·이름·스킬은 여기서 비워 둔다.

각 히어로는 이런 필드만 있으면 된다.

```text
id
name
role            # damage | tank | support
health
moveSpeed
abilities[]
  - id
  - slot        # primary | secondary | ability1 | ability2 | ultimate
  - type        # hitscan | projectile | dash | shield | heal | buff
  - damage / heal / duration / cooldown / range
model           # 나중에: 색/실루엣 플레이스홀더 → 실제 메시
```

진행 약속:

- 너는 콘셉트(이름, 역할, 스킬 감)를 준다
- 나는 그걸 위 스키마로 넣고, 플레이 가능하게 만든다
- 내가 임의로 히어로 세계관을 정하지 않는다

1단계 목표 수: **히어로 1명으로 코어를 검증**한 다음, 네가 준 목록대로 늘린다.  
최소 재미 단위는 2명(상성)이지만, 조작이 먼저다.

---

## 맵

1단계는 작은 훈련장 하나.

- 바닥 + 엄폐물 상자 + 살짝 고저차
- 스폰 2곳
- 중앙 거점 (원형 트리거) — 서 있으면 점령 게이지

시각은 로우폴리 / 단색 블록. 아트는 조작이 안정된 다음.

---

## 화면 구성

| 화면 | 하는 일 |
| --- | --- |
| 허브 (태초마을 UI) | 플레이, 히어로 선택, 이 계획서 링크/버전 |
| 히어로 선택 | 네가 넣은 캐릭터만 표시 |
| 인게임 HUD | 체력, 탄약, 스킬 쿨, 궁극기 게이지, 크로스헤어, 거점 진행 |
| 라운드 결과 | 승/패, 다시하기 |

허브는 “설정 덤프”가 아니라 **게임으로 들어가는 문**이다.

---

## 단계 (이게 일정이다)

### 0. 태초마을

- 이 문서
- Vite + Three.js 빈 장면
- GitHub Pages로 검은 화면이라도 뜨게

### 1. 몸

- 캡슐 캐릭터, 3인칭 카메라, WASD, 점프
- 바닥 위 걷기. 벽 슬라이드. 낙하

### 2. 손

- 크로스헤어, 히트스캔, 데미지, 체력, 사망/리스폰
- 더미 봇 하나 (가만히 있거나 단순 추적)

### 3. 키트

- 쿨다운, 스킬 슬롯, 궁극기 게이지
- 히어로 데이터 파일 1개 (네가 준 1번 캐릭터)

### 4. 판

- 거점 점령 또는 전멸전
- 라운드 시작/종료 UI

### 5. 얼굴

- 히어로 실루엣 구분 (색, 키, 무기 모양)
- HUD 다듬기
- Pages 배포 고정

### 6. 이후 (계획만, 구현은 미룸)

- 히어로 추가 (네 목록)
- 봇 AI 개선
- 두 번째 맵
- 로컬 2인 또는 간단한 온라인 (별도 서버 필요 — Pages만으로는 실시간 PvP 불가)

단계 번호가 건너뛰면 안 된다. 2가 흔들리면 5를 하지 않는다.

---

## GitHub Pages

- 빌드 산출물만 배포한다 (소스 전체가 사이트 루트가 되지 않게)
- 기본 URL 형태: `https://eloyprqq.github.io/vortex/`
- Three.js는 에셋 경로가 `base: '/vortex/'` 를 지켜야 한다. 로컬에선 되고 Pages에서 텍스처가 깨지는 전형적인 원인이다

멀티플레이 서버는 Pages에 못 올린다. 온라인은 나중이다.

---

## 하지 않을 것 (명시)

- 언리얼/유니티로 시작하지 않는다. 웹 배포가 우선이다
- 완성 캐릭터 모델링부터 하지 않는다
- 네트워킹부터 하지 않는다
- 스킬 20개를 한 번에 넣지 않는다
- 이 문서와 다른 “멋진 엔진 리라이트”를 충동적으로 하지 않는다. 바꾸려면 이 파일을 먼저 고친다

---

## 다음에 할 일

1. 네가 1번 캐릭터의 **역할 + 스킬 감**만 말해 준다 (이름은 가명이어도 됨)
2. 나는 단계 0–1: Vite 장면 + 걷는 3인칭 캡슐을 만든다
3. 그다음에 손(전투)을 붙인다

캐릭터를 아직 안 줘도 단계 0–1은 시작할 수 있다. 히어로는 데이터로 나중에 갈아끼운다.
